# 更新日志 (CHANGELOG)

所有重要的项目变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [2.1.0] - 2025-12-23

### ✨ 新增功能

- **TLS/DTLS 证书管理**
  - 新增证书管理页面，支持上传和管理 TLS/DTLS 证书
  - 证书存储在主机数据库（`tls_certificates` 表），从机按需拉取
  - 支持自定义注册场景文件（regScenarioFile）和最大呼叫数（regMaxCalls）配置
  - 测试启动时可选择已上传的证书用于 TLS/DTLS 协议测试

- **RTP 端口范围可选配置**
  - RTP 端口范围现在可以留空（使用系统默认端口）
  - 支持自定义最小/最大 RTP 端口范围
  - 添加端口范围验证：必须同时设置或同时留空，且最小端口必须小于最大端口

### 🐛 问题修复

- **任务统计独立化**
  - 修复主机任务历史统计包含从机任务的问题
  - 现在主机仅显示 `machine_id='master'` 的任务
  - 从机仅显示自己 `machine_id` 的任务
  - 任务历史页面标题和统计数据准确反映当前节点的任务

- **任务启动失败处理**
  - 修复从机启动任务失败时，任务仍显示在主机运行列表的问题
  - 启动失败时自动将任务状态更新为 `FAILED`
  - 记录错误信息到数据库 `error` 字段

- **文件上传功能修复**
  - 修复 TLS 证书文件上传后表单字段未填充的问题
  - 优化 `beforeUpload` 回调参数处理

- **日志上传错误修复**
  - 修复从机日志上传时 EISDIR 错误（尝试上传目录而非文件）
  - 添加文件类型检查，仅上传文件不上传目录

- **RTP 端口验证优化**
  - 修复空值和 null 值处理不一致导致的验证错误
  - 前端和后端统一 null/undefined/0 值规范化逻辑

### 🗑️ 移除功能

- **多语言支持移除**
  - 移除 `i18next` 和 `react-i18next` 依赖包
  - 删除 `/src/i18n` 目录及所有翻译文件
  - 所有界面文本改为硬编码中文
  - 日志保持英文风格（不受影响）
  - 移除语言切换组件和功能

### 📦 优化改进

- **数据库初始化脚本**
  - 将 `tls_certificates` 表添加到 `init.sql` 初始化脚本
  - 更新功能清单注释，包含 TLS 证书管理

- **UI 界面优化**
  - 合并启动测试弹窗中的重复"高级选项"标签页
  - 优化表单布局和字段分组

- **代码质量提升**
  - 统一值规范化逻辑（null/undefined/0 处理）
  - 优化错误处理和日志记录
  - 添加更详细的注释说明

### 🔧 技术变更

- **前端依赖更新**
  - 移除 `i18next@^23.7.8`
  - 移除 `react-i18next@^14.0.0`

- **数据库表结构**
  - 新增 `tls_certificates` 表
    - `id`: VARCHAR(36) PRIMARY KEY (UUID)
    - `name`: VARCHAR(255) NOT NULL (证书名称)
    - `cert_content`: TEXT NOT NULL (证书内容，PEM格式)
    - `key_content`: TEXT NOT NULL (私钥内容，PEM格式)
    - `description`: TEXT (证书描述)
    - `created_at`/`updated_at`: TIMESTAMP (创建/更新时间)

- **API 端点变更**
  - 优化 `/api/task-history` 端点，添加 `machine_id` 过滤
  - 优化任务启动失败时的错误状态更新逻辑

---

## [2.0.0] - 2025-12-22

### 🏗️ 架构重构

- **主从集群架构实现**
  - 主机（Master）节点：提供 Web UI 和任务调度
  - 从机（Slave）节点：仅提供 API 服务，执行测试任务
  - 共享 MySQL 数据库架构

- **智能负载均衡**
  - 自动选择负载最低的从机执行任务
  - 支持手动指定从机或主机执行

- **API 心跳机制**
  - 从机每10秒通过 HTTP POST 向主机报告状态
  - 实时更新 CPU、内存使用率和运行任务数
  - 自动检测从机在线/离线状态

- **文件自动同步**
  - 场景文件和注入文件从主机自动推送到从机
  - 避免手动文件复制，简化部署流程

### ✨ 新增功能

- **从机管理页面**
  - 实时显示所有从机状态（在线/离线/繁忙）
  - 监控从机 CPU、内存使用率
  - 查看从机运行任务数和历史任务数

- **配置模板管理**
  - 保存和管理测试配置模板
  - 快速复用常用配置

- **任务日志下载**
  - 一键下载任务日志（ZIP 打包）
  - 自动路由到正确的执行节点

- **应用日志下载**
  - 下载应用日志（app.log）
  - 下载错误日志（error.log）
  - 支持下载所有日志

### 🔧 技术栈

**前端：**
- React 18 + TypeScript
- Ant Design 5
- Vite 5
- Socket.IO Client

**后端：**
- Node.js 18+ + TypeScript
- Express.js
- MySQL 8.0
- Socket.IO
- Winston (日志)

---

## 类型说明

- `✨ 新增功能`: 添加的新特性
- `🐛 问题修复`: 修复的 Bug
- `🗑️ 移除功能`: 移除的功能或代码
- `📦 优化改进`: 性能优化或代码改进
- `🔧 技术变更`: 依赖更新、配置变更等
- `🏗️ 架构重构`: 架构层面的重大变更
- `📝 文档更新`: 文档相关变更
- `🔒 安全修复`: 安全漏洞修复
